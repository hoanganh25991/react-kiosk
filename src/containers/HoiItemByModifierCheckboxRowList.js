import { connect } from "react-redux"
import ItemByModifierCheckboxRowList from "../components/ItemByModifierCheckboxRowList"

const mapStateToProps = () => ({})

const mapActionToProps = dispatch => ({})

export default connect(mapStateToProps, mapActionToProps)(ItemByModifierCheckboxRowList)
